# Used by the Next.js server-side proxy routes.
# For Docker Compose keep the internal service name:
BACKEND_URL=http://backend:8000

# For local frontend development, copy this file to .env.local and use:
# BACKEND_URL=http://127.0.0.1:8000
